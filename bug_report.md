---
name: Bug report
about: A defect in parsing, integrity handling or reporting
labels: bug
---

**Do not attach real case material.** Reproduce with a synthetic profile
(`ffxforensics sample ./demo`) and modify it as needed.

If the defect could cause the tool to **misreport evidence** — a wrong
timestamp, a missed record, a misattributed search — please report it privately
first (see `SECURITY.md`), because a live case may be relying on the current
output.

### What happened

### What you expected

### Reproduction

```bash
# commands, with the synthetic profile
```

### Environment

- ffxforensics version (`ffxforensics --version`):
- Python version:
- OS:
- Firefox version the profile came from (if known):
- Timezone passed to `--tz`:

### Output

```
# paste relevant output, redacted if necessary
```
