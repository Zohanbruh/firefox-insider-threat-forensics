### What this changes

### Why

### Checklist

- [ ] `make test` passes
- [ ] `make lint` passes
- [ ] A test was added; for a bug fix, it fails without the change
- [ ] No case material, real hashes or personal data committed
- [ ] No new runtime dependencies
- [ ] `docs/SQL_QUERY_REFERENCE.md` updated if a query changed
- [ ] `CHANGELOG.md` updated

### Could this change previously produced findings?

If yes, describe how, so examiners can decide whether to re-run past
examinations.

### Does `tests/test_smoke_case029.py` still pass?

If it does not, explain why the source report's grid was wrong rather than
adjusting the test.
